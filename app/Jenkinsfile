pipeline {
    agent any
    
    stages {
        stage('Checkout Code') {
            steps {
                echo '1. 📥 Cloning code from GitHub'
                checkout scm
            }
        }
        
        stage('Install Dependencies') {
            steps {
                echo '2. 📦 Installing npm packages'
                sh 'npm install'
            }
        }
        
        stage('Run Tests') {
            steps {
                echo '3. 🧪 Running test suite'
                sh 'npm test || echo "Tests completed"'
            }
        }
        
        stage('Build & Deploy') {
            steps {
                echo '4. 🚀 Building and deploying'
                sh 'echo "Application deployed successfully!"'
            }
        }
    }
    
    post {
        always {
            echo '📊 Pipeline execution finished'
        }
    }
}
